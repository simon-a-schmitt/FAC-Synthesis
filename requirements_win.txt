# Windows-compatible dependencies for fac_test_pipeline_quant.
# Keep this list minimal and avoid Linux-only packages (e.g., uvloop, flash-attn, deepspeed).

torch>=2.1.0
transformers>=4.45.0
huggingface-hub>=0.34.4
accelerate>=0.33.0
sentencepiece>=0.2.0
tqdm>=4.66.0

# Runtime dependencies commonly required by transformers stacks on Windows
numpy>=1.26.4
tokenizers>=0.19.1
safetensors>=0.6.0
packaging>=25.0
requests>=2.32.5
filelock>=3.19.1