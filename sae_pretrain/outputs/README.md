# Model Weights

To support reproducibility, we will release the **training weights** for **three model families** after the paper is accepted.  
Due to the large file size, the weights will be provided via a **cloud drive download link** rather than being hosted directly in this repository.
